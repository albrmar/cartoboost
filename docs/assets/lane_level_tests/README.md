# Lane-Level Diagnostics

This directory contains generated lane-level acceptance evidence. The maintained
runbook is [Lane-Level Acceptance](../../benchmarks/lane-level.md).

Use these plots and metrics to inspect route-cell, temporal, spatial, and
combined lane behavior. Refresh them only from
`scripts/run_lane_level_acceptance_metrics.py`.
