# Changelog

## Unreleased

- Added CartoBoost Forecasting V1: `ForecastFrame`, deterministic
  `ForecastResult` outputs, local statistical forecasters, rolling-origin
  backtesting, leakage-safe lag features, `CartoBoostLagForecaster`, weighted
  ensembles, artifact/config helpers, CLI script support, examples, docs, and a
  deterministic forecasting benchmark harness.
