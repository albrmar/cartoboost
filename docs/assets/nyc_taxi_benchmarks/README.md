# NYC Taxi Benchmark Assets

This directory is the default output location for
`scripts/run_nyc_taxi_quality_benchmarks.py`.

Generated files include `results.json`, `results.md`, `metric_summary.png`, and
per-task plots under `plots/`. Raw TLC Parquet files are cached under
`data/nyc_taxi/` and are intentionally not committed.
