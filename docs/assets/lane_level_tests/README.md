# Taxi Zone Diagnostics

This directory contains generated taxi-zone acceptance evidence. The maintained
runbook is [Taxi Zone Acceptance](../../benchmarks/taxi-zone.md).

Use these plots and metrics to inspect pickup/dropoff, temporal, spatial, and
combined split behavior. Refresh them only from
`scripts/run_lane_level_acceptance_metrics.py`.
