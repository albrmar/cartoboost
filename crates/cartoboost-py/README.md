# CartoBoost Native Extension

`cartoboost._native` powers the Python estimator features used for
temporal-spatial modeling: spatial splitters, periodic splitters, sparse-set
features, fuzzy routing, linear leaves, and JSON artifact loading.

```bash
uv run maturin develop
```

The Python estimator requires this extension.
